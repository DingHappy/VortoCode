# VortoCode 环境变量配置

# One API 网关配置
OPENAI_API_BASE=https://relay.dinghappy.com/v1
OPENAI_API_KEY=sk-你的令牌

# 可选：Anthropic 兼容接口
ANTHROPIC_BASE_URL=https://relay.dinghappy.com
ANTHROPIC_AUTH_TOKEN=sk-你的令牌

# 默认模型
DEFAULT_MODEL=mimo-v2.5

# 工具调用协议：**默认原生 function-calling（native）**——对支持的模型更可靠（真机 dogfood：
# native 3/3 正确落地 vs 提示式 1/3，提示式下模型易把工具结果误当用户消息、凭空编造交付）；模型
# 不支持会自动回退提示式（模型无关兜底）。三端统一。要强制提示式：置 0。
# VORTOCODE_NATIVE_TOOLS=0

# 记忆系统
MEMORY_STORAGE_PATH=.vortocode/memory

# 日志
LOG_LEVEL=INFO
LOG_FILE=.vortocode/logs/app.log

# Web 服务器
WEB_HOST=0.0.0.0
WEB_PORT=8000
