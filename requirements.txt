# VortoCode 依赖

# 核心依赖（须与 pyproject.toml [project].dependencies 对齐）
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
pyyaml>=6.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
psutil>=5.9.0         # 系统指标（core/monitoring 等）；此前漏在 requirements 里
jedi>=0.19           # 语义代码导航（find_definition/find_references）；漏了则语义导航静默降级
markdown>=3.4         # 制品(artifact) markdown → HTML 渲染

# 可选依赖
# qdrant-client>=1.7.0  # 向量数据库
# openai>=1.0.0  # OpenAI API
# anthropic>=0.18.0  # Anthropic API

# 开发依赖
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
black>=23.0.0
ruff>=0.1.0
