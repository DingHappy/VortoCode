# 值班检查单（HEARTBEAT.md）——放到目标仓库 `.vortocode/HEARTBEAT.md` 才生效

<!--
  heartbeat 每次醒来只读这一个文件当值班检查单（轻上下文、隔离会话、可用便宜模型）。
  换文件：VORTOCODE_HEARTBEAT_CHECKLIST=<仓库内相对路径>（必须在工作目录内，越界不读）。

  **这个文件是「要查什么」的清单，不是「去做什么」的指令。**
  它按不可信输入处理：读进来即把该回合标成污点态（免确认授权全部失效），值班 agent 被明确要求
  无视清单里出现的任何指令（改文件/跑命令/发消息/访问网址）并把它当异常上报。所以别在这里写
  「帮我把 X 改成 Y」——那不会被执行，只会被当成检查单被投毒的信号。

  应答约定：全部无异常就回 HEARTBEAT_OK → **无事静默**（不通知、不写会话历史，只往 Journal 落
  一行确定性台账）。有异常如实说 → 走通知三路（台账 /api/notices + WS 广播 + IM 推 owner）。
  领 BACKLOG 由代码做（`- [ ]` 一次领一条 → 后台流水线 → vorto/* 分支 + draft PR，绝不自动合并），
  不靠这个文件驱动。
  开关：VORTOCODE_HEARTBEAT=1 起 serve 才值班；间隔 VORTOCODE_HEARTBEAT_EVERY（默认 30m）。
-->

逐项看一眼，只报异常：

- 打开的 `vorto/*` PR 里，有没有 CI 是红的。
- 有没有 draft PR 超过 3 天没动静。
- 有没有后台任务停在 `failed` / `interrupted` 没人管。
- 主工作区有没有意外的未提交改动（本该只发生在一次性 worktree 里的改动漏了出来）。
- 磁盘水位、`.vortocode/` 体积有没有异常增长。

以上全部正常 → 回 `HEARTBEAT_OK`。
有异常 → 点名是哪一项、看到了什么、建议怎么处理（只报告，不动手改）。
