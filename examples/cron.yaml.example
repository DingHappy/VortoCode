# VortoCode cron 作业表示例——放到目标仓库的 `.vortocode/cron.yaml` 才生效。
#
# 调度循环默认**关**：仅当以 opt-in 方式起服务才跑（不擅自跑自主 LLM 作业）——
#   VORTOCODE_CRON=1 vortocode server      # 起 cron
#   VORTOCODE_HEARTBEAT=1 vortocode server # 起 heartbeat
# 也可手动触发单个作业（不起常驻）：vortocode cron run <name>
#
# schedule 支持三种：
#   at HH:MM          每天该时刻一次（如 at 02:30）
#   every Nm|Nh|Nd    固定间隔（如 every 6h）
#   5 段 cron 子集     m h dom mon dow（支持 * / 数字 / */N / a,b / a-b；dow 0=周日）
# announce: im（把结果推到 IM）| silent（只落台账）

jobs:
  # 夜跑评测（B3）——每天凌晨对最新基线跑回归对比（诚实率/落地率降 = 退出码非零 = 硬回归）。
  - name: nightly_evals
    schedule: "at 02:30"
    prompt: >
      跑评测回归：先 ls evals/baselines/ 找**最新**的基线 json，然后
      python -m evals --compare evals/baselines/<最新文件>。退出码非零说明有硬回归
      （诚实率/落地率下降）——把逐场景 Δ 表和回归项原样报给我；全绿就说一声全绿。
    model: mimo-v2.5           # 可用便宜/强模型分工（需中转站已授权该型号）
    announce: im
    enabled: false             # 默认关，确认要夜跑再改 true

  # CI 红自动修（C3）——扫描本仓库打开的 vorto/* PR，CI 红的用 pr_fix 修。默认关。
  - name: ci_red_autofix
    schedule: "every 30m"
    prompt: >
      列出本仓库所有打开的 vorto/* 分支的 PR，对其中 CI 检查为红的，
      调 pr_fix(pr=<号>) 按 review 评论 + CI 失败在该分支上修正并 push。
      只碰 vorto/* 分支，绝不动 main。修完把结果告诉我。
    announce: im
    enabled: false             # 默认关（避免意外自动改动），确认要开再改 true

  # 依赖升级检查——每周一早上看看有没有依赖要升。
  - name: dep_check
    schedule: "0 9 * * 1"
    prompt: "看看 requirements.txt / pyproject 有没有明显该升级的依赖，列个清单（先别动手）。"
    announce: im
    enabled: false

  # 信号采集——**确定性作业、零 LLM**。抓 HN / GitHub 新仓库 / 官方博客 RSS / V2EX，
  # 算分数与 star 的**增量**（50k star 的仓库不是新闻，这周涨 2k 的才是）、跨天去重，
  # 落成一条 signals 产出物给 scout 读。
  # 它必须排在 pipeline_tick **前面**跑：scout 读的是最新那条 signals。
  # 退出码：0=采到了（个别源失败也算）；1=一条都没采到。个别源挂掉不报红——
  # 那会让台账里全是红的、人反而不看了；失败的源名会进产出物，scout 据此知道自己看到的是一部分。
  - name: signal_collect
    schedule: "at 08:30"
    command: "vc collect --quiet"
    announce: silent
    enabled: false             # 默认关，确认要采再改 true

  # 运营流水线定时推进——**确定性作业，零 LLM**（LLM 只在工序内部上场）。
  # `tick` 扫一遍所有活着的运行各推一道工序，状态有变化才通报：挂三天的等批只说一次，
  # 不会每半小时刷你一条。对外工序（发布）它连试都不试——无人值守没有确认通道，
  # 试三次只会把重试次数烧光把自己废掉；它会推一条"这一步要你带授权来"。
  # 退出码恒 0 是**故意的**：该说的话已经从通知通道说过了，再让 cron 报一次红就是重复通报。
  - name: pipeline_tick
    schedule: "every 2h"
    command: "vc pipeline tick"
    announce: silent           # 通报由 tick 自己按状态变化发，别再叠一层"作业跑完了"
    enabled: false             # 默认关，配好 .vortocode/pipelines/*.yaml 再改 true
