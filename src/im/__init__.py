"""IM 通道桥（D2）——把主 agent 搬上 IM：手机发任务 → 流水线跑 → 进度推送 → 内联按钮确认 → 回报。

「人在两个关口」（需求确认、PR 合并）恰是 IM 消息的完美形态：走在路上也能派活、审批。设计：
- **通道无关的 bridge 核心**（bridge.py）持有会话/确认/进度/配对/串行回合逻辑；
- 各 **ChannelAdapter**（telegram.py …）只做「收发 + 内联按钮」的 transport 翻译。
桥是三端同源工具工厂的**第四端**——走 build_agent_tools + skill_catalog，不手写工具串（防漂移）。

安全底线（OpenClaw 一周被扫出 2.1 万暴露实例的教训）：
- **配对制**：只服务显式配对的主人（owner_id），其它一律静默忽略并计数。
- **纯出站**：Telegram getUpdates 长轮询是出站连接，gateway 无需入站暴露端口。
- **每次确认独立**：IM 端不提供「本会话始终允许」——摄入外部内容后的对外操作照样逐次确认（配合 D0 污点）。
"""
